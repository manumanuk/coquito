<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="customTextAppearance.Widget.ActionBar.Title" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textSize">25dp</item>
        <item name="android:fontFamily">sans-serif-thin</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:shadowColor">#ffffff</item>
        <item name="android:shadowDy">3</item>
        <item name="android:textStyle">normal</item>
        <item name="android:layout_centerHorizontal">true</item>

    </style>
    <style name="actionBarTheme" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/colorPrimary</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:titleTextStyle">@style/customTextAppearance.Widget.ActionBar.Title</item>
        <item name="titleTextStyle">@style/customTextAppearance.Widget.ActionBar.Title</item>
        <item name="android:homeAsUpIndicator">@style/customTextAppearance.Widget.ActionBar.Title</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>

    <style name="HomeTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/actionBarTheme</item>
        <item name="android:actionBarStyle">@style/actionBarTheme</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
